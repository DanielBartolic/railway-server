[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn webhook_server:app --bind 0.0.0.0:$PORT --workers 2 --access-logfile - --error-logfile -"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
